# 2026-09-02 작업 기록

이 문서는 오늘 하루(2026-09-02) `grippers`(Pi 쪽 ROS2 워크스페이스)와
`grippers-host-mac`(맥 Host 제어) 두 저장소에서 있었던 실기 대응, 도구
수정, 실기검증이 필요한 항목, 그리고 세션 중 사용자와 합의한 규칙을 한
곳에 모은다. 다음 세션이 이 문서만 보고도 오늘 무슨 일이 있었는지, 뭘
아직 확인 안 했는지 알 수 있는 것이 목표다.

## 작업 규칙 (다음 세션이 잃어버리기 쉬운 것들)

- **Pi SSH**: `ssh pi@192.168.0.7` (hostname `raspberrypi`, mDNS 폴백은
  `ssh pi@raspberrypi.local`). **IP는 DHCP라 계속 바뀐다** — 오늘도
  `10.82.133.189`에서 `192.168.0.7`로 이미 한 번 바뀌어 있었고, 이걸
  모르고 낡은 IP로 붙으면 SSH가 그냥 조용히 실패해서 원인 추적이 오래
  걸린다(아래 "mac_camera_view 대장정" 참고).
- **컨테이너 진입**: Pi 자신의 쉘에서 `~/docker/exec_shell.sh` 실행
  (`docker exec -it -u ubuntu IntelPi /bin/zsh`를 감싼 것). **반드시 진짜
  TTY가 있는 인터랙티브 터미널에서 실행해야 한다** — 비대화형 SSH로
  실행하면 `cannot attach stdin to a TTY-enabled container` 에러가 난다.
- **쉘 방언**: 컨테이너 안은 zsh다. ROS 설정은 `setup.zsh`를 source해야
  한다 — zsh에서 `setup.bash`를 source하면 `${BASH_SOURCE}` 기반 경로
  로직이 깨진다.
- **ROS_DOMAIN_ID**: 항상 `21`. 예외 없음.
- **명령문 블록에 `#` 주석을 달지 않는다** — 이 사용자의 zsh에 붙여넣을
  때 깨진다.
- **바인드 마운트**: 호스트 `~/docker/shared/grippers` = 컨테이너
  `/grippers`, 그 안 `third_party/`는 컨테이너 `/third_party`, `ros2_ws/`는
  컨테이너 `/ros2_ws`에도 따로 마운트된다. Pi 쪽 이 클론은 맥 클론
  (`~/Desktop/intel/grippers-baseline-wt`)과 **독립적** — 새 스크립트는
  `scp`로 옮겨야 Pi에 반영된다.
- **grippers 저장소(Pi 쪽) 워크플로**: 커밋 → `origin`(팀 저장소
  grippers-intel/grippers)과 `personal-mirror`(kica927/grippers) 둘 다
  push → PR은 `kica927/grippers`(개인 미러)에만 열고 그쪽에서 병합. 팀
  저장소(`grippers-intel`)에는 절대 직접 병합하지 않는다. 브랜치/PR
  헤드는 항상 `kica927/baseline_mission`, base는 `main`.
- **grippers-host-mac 워크플로**: 원격이 `kica927/grippers-host-mac`
  하나뿐이라 PR 없이 `main`에 바로 커밋·push.

## 오늘 있었던 일

### 1. 오전 실기 대응 (이 세션 요약 이전, 07:35~11:45경 — `grippers-host-mac`)

커밋만 남기고 상세 경위는 이 세션에 없다. 제목으로 요약:

- `d0dacee` GRASP_FORCE·RETURN_HOME이 `_STATE_TO_PI`에 없어 조용히
  IDLE로 대체되던 락업 수정
- `8d40eaf` GRASP_REPLAN 재정렬 반복 시 오버헤드 카메라로 다시 세움
- `e99bde1` GRASP 후 무단 후진 금지, 회전 목표각 히스테리시스
- `fa4eb3d` 포즈를 잃는 순간 이동 명령이면 즉시 stop 전송
- `5bab962`, `193c74c` NUDGE_BOX 실시간 라이다 정지, 오검출 basket_fix 무시
- `8729579` APPROACH_PIECE 왼쪽 편향 보정 + 정밀 겨눔 게이트
- `174ec3d` 바구니 앞 yaw 보정 실제 실행 (10:18 실기 락업 수정)
- `ed8acb8` PLACE에서 바구니 완전 미검출 시 REACQUIRE

### 2. 11:47 실기 — 바구니 돌진 재발 → NUDGE_BOX 정체시간 단축

11:47:12 미션 로그에서 차량이 또 바구니에 돌진했다. 처음엔
`BASKET_DISTANCE_DEADBAND_M`이 좁다고 오진단했다가, `mission_config.py`의
설계 주석(겨냥점은 Pi 수용창의 **중앙**을 노리도록 일부러 여유를 둔
것)을 다시 읽고 스스로 정정했다 — 관측된 `0.155m → 0.139m` 수렴은 버그가
아니라 정상 동작이었다.

진짜 원인: `BASKET_NUDGE_STALL_SEC=10.0`이 "바구니에 막혀서 못 나가는"
상황치고 너무 관대했다(실측 ~9.6초 계속 밀어붙임, 정상적으로 필요한
시간은 최대 ~5초). 원래 10초는 **다른 고장 모드**(2026-08-28 모터 전원
완전 손실)에 맞춰 잡은 값이었다.

**수정**(`grippers-host-mac`, 커밋 `6f88d32`): `BASKET_NUDGE_STALL_SEC`
10.0 → 6.0. `FakeClock` 기반 테스트 3개 추가
(`tests/test_nudge_box_blocked_stall.py`). 전체 스위트 132 passed, 1
skipped.

### 3. EEPROM / 캘리브레이션 가드 (`grippers`)

- 그리퍼(서보6)를 한계까지 좁혔을 때의 RAW 값(1106)을 확인, 예전 캘리브레이션
  값과 비교하는 과정에서 **Min/Max_Angle_Limit 레지스터가 Homing_Offset과
  별개로 존재하고, 기존 캘리브레이션 가드·복구 도구가 이걸 전혀 안 본다**는
  걸 발견했다 — 2026-09-01 "그리퍼 완전 먹통" 사고의 실제 원인이었는데
  그동안 Pi에서 임시방편 스크립트로만 고쳐져 있었다.
- **수정**(커밋 `c6c3d16`, PR #56): `position_limit_registers.py` 신규
  작성(벤더 SDK의 private `_read_u16`/`_write_u16`/`set_eeprom_lock`을
  직접 사용 — Min/Max_Angle_Limit엔 Homing_Offset과 달리 공개 API가
  없다), `arm_driver_node.py`의 기동 가드와 `restore_taught_offsets.py`
  복구 도구 양쪽에 검사·복구 로직 추가. 테스트 12개 신규 + 기존 파일
  보강. 전체 스위트 520 passed, 3 skipped.
- **그리퍼 재보정 준비**(커밋 `5e096a0`, PR #55): 사용자 지시로 1106보다
  낮은 값 재보정을 대비해 `gripper_force_probe.py`의 `--holding` 스윕
  하한을 `EMPTY_SWEEP_MIN_MM`(2.0, 기존)과 `HOLDING_SWEEP_MIN_MM`
  (-20.0, 신규)으로 분리 — 원래 공유 하한(2.0mm)이 이미 배포된 프로덕션
  하한(0.0mm)보다 높아서 `--holding`이 스윕을 0걸음만 하고 끝나던 문제도
  같이 고쳤다.
- **팀원(sysy009) 저장소 확인**: sysy009의 실제로 push된 브랜치는 전부
  2026-08-28 이전에 멈춰 있어, 이 EEPROM 사태(08-29~09-02) 전체가
  거기엔 존재하지 않는다 — VLA/baseline 전환 시 EEPROM이 어떻게 반응하는지
  그 저장소만으로는 답할 수 없었다(사용자가 자신의 `kica927/smolVLA`
  브랜치는 한 번도 안 써봤다고 확인해줬으므로 그쪽은 배제하고 판단).

### 4. PLACE 완료 → RETURN_HOME (시연용, `grippers-host-mac`)

사용자 지시: "INSERT 시 RETURN_HOME으로 해서 같은 과정을 돌리는 형태로
해보자." PLACE 완료 시 곧장 `SEARCH_TARGET`으로 가지 않고, 기물을
포기했을 때(`_skip_target`)와 같은 경로인 `RETURN_HOME`을 한 번 거치도록
바꿨다 — 매 라운드가 항상 같은 자리(`DEFAULT_HOME_XY`)에서 시작해 시연이
일정하게 보이게 하려는 목적이다.

**수정**(커밋 `456e535`): `mission.py`의 PLACE 완료 분기를
`State.RETURN_HOME`으로. 이 때문에 `test_basket_close_loop.py`,
`test_instruction.py`의 `MAX_STEPS`가 늘어난 사이클을 못 버텨 깨졌는데,
디버그 스크립트로 추적한 결과 FSM이 멈춘 게 아니라 442사이클 걸려 정상
완주하는 것으로 확인 — `MAX_STEPS`를 800/900으로 올리고, PLACE 완료
순간과 NUDGE_BOX 보정 왕복을 구분하는 `_run_to_place_done()` 헬퍼를
추가했다. 신규 `test_place_returns_home.py` 3개 포함, 최종 135 passed,
1 skipped.

**확인된 것**(이번 세션에서 재검증): `RETURN_HOME`도 다른 구간과 똑같이
`GridPathPlanner.update()`를 거쳐 차량 반경(`ROBOT_RADIUS_PIECE_M`)과
다른 기물 위치(`_other_pieces(piece_map)`, `PIECE_OBSTACLE_RADIUS_M` +
`OBSTACLE_MARGIN_M`)를 감안해서 경로를 짠다 — 별도 예외 없음.

### 5. mac_camera_view 대장정 — 클래스별 색상부터 프로덕션 게이트 변경까지

**5-1. YOLO 오버레이 실시간 뷰어 신규**(커밋 `31fc080`, PR #57): 기존
`mac_camera_view.py`(Pi 카메라를 SSH로 당겨와 맥에서 보는 도구)에
`--live` 모드 추가 — 맥 로컬 OpenCV 창, `s`로 캡처, `q`/Esc로 종료.

**5-2. 클래스별 색상 + 큰 글자**(커밋 `4f474c2`, PR #58): 6개 클래스
(rook/knight/queen/soccer/box/star)에 각각 다른 색(전부 빨강 계열 제외 —
사용자의 기존 "OpenCV에서 빨간 계열 쓰지 말라" 지시를 따름). 게이트
통과/탈락 구분은 색 대신 테두리 굵기(굵게/얇게)로 옮김. 글자 크기
0.44→0.85.

**5-3. 맥 직결 뎁스캠 시도와 좌절**: 사용자가 같은 뎁스캠(ASJ ZNX_NVT,
Novatek 칩셋)을 Pi 대신 맥에 USB 허브로 직결. macOS가 표준 UVC 카메라로
인식은 했고(`cv2.VideoCapture`는 실패, `ffmpeg -f avfoundation`은 성공),
`grippers-host-mac/host/mac_local_yolo_view.py`를 새로 만들어 로컬
YOLO(train-9, 로컬 sha256 대조로 배포본과 동일 확인) 검출까지 한 번은
성공시켰다. **그런데 이후 반복 시도부터 카메라가 응답을 멈췄다** — 최소
해상도로도 안 됨(대역폭 문제 아님), FaceTime은 계속 정상(macOS 전체
잠김 아님), `VDCAssistant` 재시작도 무효, 완전 재연결 후 단발 시도도
무효. **결론: 이 카메라는 이 허브 경유 macOS 환경에서 근본적으로
불안정 — Pi 경유(`ascamera` 벤더 SDK)로 되돌림.** 카메라를 다시 Pi에
꽂아 정상 확인.

**5-4. Pi IP 낡음 버그**(커밋 `07e3ada`, PR #59): `mac_camera_view.py`에
`PI = "pi@10.82.133.189"`가 하드코딩돼 있었다 — 오늘 초 이미
`192.168.0.7`로 바뀐 뒤였는데 아무도 못 알아챈 채였다. SSH가 조용히
실패해 `--live`가 "프레임을 못 받았습니다"로만 보였다.

⚠️ **디버깅 중 잘못 짚은 것들** (기록에 남긴다 — 사용자 지시
"바뀐 것을 제대로 기록해두라"): IP 문제를 못 찾은 채로 `docker exec -i`
플래그, stdin 파이프 전달 방식, `stderr` 미드레인으로 인한 ssh 채널
흐름제어 정체를 차례로 의심하고 실제로 코드를 고쳤었다(파일 scp 실행
방식으로 전환, `-i` 제거, stderr 드레인 스레드 추가). 전부 **틀린
진단**이었다 — 진짜 원인은 내가 디버깅용으로 짠 테스트 스크립트가
`run_grabber("depth", ...)`처럼 짧은 CLI 키를 실제 ROS 토픽 경로 대신
그대로 넘긴 내 테스트 하네스 버그였다(`main()` 자체는 처음부터
`TOPICS[args.topic]`으로 올바르게 변환하고 있었다). 원인을 찾은 뒤
불필요해진 변경은 전부 원복하고, 진짜 필요했던 IP 수정 하나만 남겼다.

**5-5. --live 캡처가 실제로는 저장 안 되던 버그**(커밋 `0bc0472`, PR
#61): 사용자가 6장 "저장"됐다고 터미널에서 확인했는데 실제 파일이
하나도 없었다. `topic`이 `/depth_cam/rgb/image_rotated`처럼 `/`로
시작하는 실제 ROS 경로라, `capture_dir / f"{topic}_..."`로 이어붙이면
pathlib이 이를 절대경로로 취급해 `capture_dir`을 통째로 무시 —
파일시스템 루트 밑에 쓰려다 권한 없어 조용히 실패했는데
`cv2.imwrite`의 반환값을 안 봐서 "저장됨" 메시지가 그대로 찍혔다. 파일
이름에서 `/`를 제거하고, `imwrite` 실패 시 명확히 에러를 찍도록 수정.
`grippers-host-mac/host/mac_local_yolo_view.py`에도 (경로 자체는
안전했지만) 같은 예방 차원으로 반영.

**5-6. 신뢰도 필터**(커밋 `3533339`, PR #60): "신뢰도 0.7 이하면 검출
띄우지 마" — 저신뢰도 검출은 아예 안 그리게. 위치 게이트만으로 걸러진
것(신뢰도는 충분한데 화면 아래쪽이 아닌 경우)은 여전히 얇은 테두리로
보여주는 목적은 유지.

**5-7. 화면위치 게이트선 비교 → 프로덕션 값 변경**: 사용자가 뷰어의
게이트선(원래 perception_node.py의 실제 값을 그대로 반영하던 290px)을
눈으로 비교해보며 350 → 200 → 240 순으로 시험(PR #62, #63, #64) 후,
최종적으로 **250px를 프로덕션 값으로 확정**(아래 6번 항목).

### 6. observe_target 화면위치 게이트 290 → 250px (프로덕션 변경, `grippers`)

**변경**(커밋 `61a09f0`, PR #65): `perception_node.py`의
`OBSERVE_MIN_BOTTOM_Y_PX` 290.0 → 250.0.

**사용자가 댄 근거**:
1. 지금 뎁스카메라가 30cm 넘게 떨어진 물체를 잘 못 본다 — 290은 그보다
   가까운 거리대만 통과시켜 실제로 볼 수 있는 물체까지 자주 걸렀다
   (2026-09-01 실기: conf 0.97로 정확히 잡은 진짜 물체가 이 게이트에
   걸려 observe_target이 "못 찾음"으로 답한 사고와 부합).
2. train-9로 검출 모델이 바뀌며 recall이 크게 올랐다(0.862→0.956,
   perception_node.py 자체 벤치마크 비교 주석) — 신뢰도 게이트가 배경
   오검출을 더 잘 막아주니 위치 게이트가 조금 헐거워져도 된다는 판단.

**영향받는 경로 세 곳** (전부 `ObserveTarget` 서비스 → 이 게이트를
통과):
- `identify_target()` — GRASP 진입 판정(`_judge_grasp`)·정렬 판정
  (`_judge_alignment`)이 씀. 화면 위쪽(먼 거리)까지 후보로 더 넓게
  인정하게 된다.
- `remember_target()` — 파지 직전 기준 관측 기록. 이게 실패하면
  `confirm_grasp`가 아예 작동 못 한다.
- `confirm_grasp()` — "물체가 화면에서 사라짐"을 파지 성공으로 해석.
  게이트가 좁으면 물체가 실제로는 그대로 있는데 위치 게이트 밖으로만
  밀려도 "사라짐"으로 오판할 위험이 있었는데, 게이트가 넓어지며 그
  위험이 줄어든다.

**같이 맞춘 것**: `tools/grasp_geometry_calibrate.py`의 동일 상수 사본,
`mac_camera_view.py`/`mac_local_yolo_view.py` 뷰어 게이트선. 이 두
사본이 다시 갈라지지 않도록 `tests/test_constant_copies.py`에
`test_화면위치_게이트_사본이_같다` 회귀 테스트 신규 추가 — 이 저장소는
이런 사본 불일치로 이미 여러 번(K값, 턱선 JAW_LINE_FOR_HINT) 실제
사고를 겪은 전례가 있다. 전체 스위트 521 passed, 3 skipped.

### 7. Pi 프로세스 정리 — 중복 세대·시리얼 포트 충돌

카메라를 Pi에 다시 물린 뒤 `mac_camera_view.py`를 테스트하다가, 오늘
`bringup.launch.py`가 여러 번 재시작되며 `joint_state_publisher`·
`ros_robot_controller`·`odom_publisher`가 **5세대까지 동시에 살아
있는 것**을 발견했다(09:43/10:17/10:53/11:26/11:28 시작). 또
`depth_cam_rotate_node`가 CPU를 거의 100% 계속 쓰고 있었다.

사용자 확인("Pi로 작업 중인 것 없다") 후 정리 진행:
- 오래된 세대 전부 SIGTERM → 응답 없어 SIGKILL(둘 다 사용자가 직접
  실행 — `kill` 계열 명령은 Claude Code 권한 분류기가 자동 차단해서
  대필 불가)
- `bringup.launch.py` 정확히 같은 인자(`use_fake_base:=false
  use_fake_arm:=false use_fake_perception:=false
  host_ip:=192.168.0.9`)로 컨테이너 안 detached 재기동
- 재기동 로그에서 SO-ARM101(`/dev/soarm`) 연결 실패 발견 →
  **정상**(팀원이 팔 하드웨어 작업 중이라 의도적으로 뽑아둔 상태)
- `ros_robot_controller`의 `SerialException: multiple access on port?`
  발견 → 아침 07:01부터 떠 있던 **별개의** `ros_robot_controller`
  (PID 2054/2056, bringup 트리 밖)가 `/dev/rrc`를 붙잡고 있던 게 원인
  — 사용자 확인 후 이것도 종료. 이후 LiDAR 10Hz, 뎁스캠 13~14Hz로
  안정화 확인.

### 8. 뎁스카메라 발열 — 미해결, 물리적 조치 권고

사용자가 뎁스카메라 발열을 보고. `ascamera_node`는 launch 파일에
`respawn=True`가 걸려 있어 SSH로 그 프로세스만 죽여도 몇 초 안에
자동으로 다시 뜬다 — 소프트웨어로 "그것만" 끄는 게 사실상 안 된다.
전체 `bringup.launch.py`를 내리는 건 로봇 스택 전체(베이스·팔·미션
오케스트레이터)를 같이 내리는 큰 조치라 카메라 발열 대응치고는
과하다고 판단, **USB 케이블을 물리적으로 뽑는 것**을 권고했다. 이
문서 작성 시점까지 사용자의 후속 확인은 없음 — 다음 세션에서 확인
필요.

## 실기검증이 필요한 것 (체크리스트)

- [ ] **observe_target 게이트 250px 재검증** — 여유가 50px→10px로
      줄었다. 문서화된 배경 오검출 5개(벽 밑동·바구니 주변)의 최댓값이
      정확히 240px라 250은 거기서 겨우 10px 위다. train-9 배포 후 이
      5개가 지금도 재현되는지 `tools/grasp_geometry_calibrate.py
      --mode gate`로 재확인 안 함.
- [ ] **RETURN_HOME 경유 PLACE 완료**(전체 시연 루프)를 실기로 아직
      안 돌려봄 — 시뮬레이션(Fake 어댑터)으로만 442사이클 완주 확인.
- [ ] **NUDGE_BOX 정체시간 6초** — 시뮬레이션 테스트로만 확인, 실제
      바구니에 막히는 상황 재현 안 함.
- [ ] **그리퍼(서보6) Min/Max_Angle_Limit 재보정** — 1106보다 낮은
      값으로. `gripper_force_probe.py --holding <label> --min-mm -20.0`
      준비는 됐으나 실기 미실행. 오늘 그립 힘 튜닝 작업 자체가 같은
      EEPROM 문제로 영향받았을 가능성도 사용자가 제기 — 재보정 후
      그립 힘도 다시 확인 필요.
- [ ] **캘리브레이션 가드/복구 도구의 Min/Max_Angle_Limit 검사**를
      실기 arm_driver_node 기동 시 실제로 통과하는지 미확인(코드
      리뷰·단위 테스트만 완료).
- [ ] **뎁스카메라 발열** — 물리적 원인(정상 발열 vs 이상 과열)
      미확인. USB 재연결 후 온도 재확인 필요.
- [ ] **맥 직결 뎁스카메라**는 이 세션에서 포기 — 다시 시도할
      가치가 있는지는 사용자 판단에 맡김(허브 자체를 바꿔보는 것
      정도가 남은 유일한 미시도 경로).
- [ ] 오전 커밋들(`fa4eb3d`, `5bab962`, `193c74c`, `8729579`,
      `174ec3d`, `ed8acb8`) — 이 문서 작성 시점 기준 실기 검증 여부를
      이 세션에서 추적 못 했다. 다음 세션에서 실기 로그로 확인 권장.

## 우리끼리 정한 것들 (표준 관행으로 굳어진 결정)

- **OpenCV 시각화에서 빨간 계열 색은 웬만하면 쓰지 않는다** — 이번
  세션 이전부터의 표준 지시, 오늘 새 도구(`mac_local_yolo_view.py`)에도
  그대로 적용.
- **디버깅 중 잘못된 진단으로 코드를 고쳤다가 원인을 찾으면, 불필요해진
  변경은 원복하고 진짜 원인 수정만 남긴다** — 오늘 IP 버그 사례가
  이 원칙의 실례. 진단 과정 자체(뭘 의심했고 왜 틀렸는지)는 지우지
  않고 기록에 남긴다.
- **뷰어(디버깅 도구)의 게이트값은 프로덕션 값과 항상 같아야 한다**는
  것이 `mac_camera_view.py`의 원래 설계 전제 — 비교 실험 중에는
  일시적으로 갈라져도 되지만, 실험이 끝나면 반드시 다시 맞춘다(오늘
  350/200/240 비교 후 최종 250으로 재동기화).
- **물리적 위험(과열 등)은 소프트웨어 조치보다 물리적 조치(케이블 분리
  등)를 우선한다** — `respawn=True`가 걸린 노드처럼 소프트웨어만으로는
  확실히 끌 수 없는 경우가 있기 때문.
- **로봇의 모터 제어 프로세스에 영향을 줄 수 있는 `kill` 등은 사용자가
  직접 실행한다** — Claude Code의 권한 분류기가 원격 프로세스 강제
  종료를 자동 차단하므로, 정확한 명령을 사용자에게 안내하고 실행은
  맡긴다.
- **팀원이 Pi 하드웨어(팔 등)를 직접 만지고 있을 때도, 그 부분과
  겹치지 않는 다른 작업(카메라·베이스 등)은 사용자 확인을 받으면
  진행해도 된다** — 오늘 실제로 이렇게 진행했다(팔은 팀원 작업 중이라
  손대지 않고, 베이스/카메라 프로세스 정리는 사용자 승인 후 진행).
