"""Pi 미션의 수치 상수 — 실측 완료분과 TODO를 한곳에 모은다.

값의 출처를 세 등급으로 구분한다. 등급을 섞어 쓰면 무엇이 검증된 값이고
무엇이 지어낸 값인지 알 수 없게 된다.

  실측  — 하드웨어로 재서 확인한 값. 그대로 믿어도 된다.
  지시  — 사용자/팀이 정한 설계값. 실측은 아니지만 결정된 값이다.
  TODO  — **아직 모른다.** `None`으로 두어 쓰는 쪽이 반드시 걸리게 한다.

TODO를 0이나 그럴듯한 숫자로 채우지 않는 이유는 "모르면 실패"라는 이 저장소의
관례와 같다 — 지어낸 값으로 도는 것처럼 보이게 만들면 실기에서 원인을 못 찾는다.

⚠️ 2026-08-26 팀 확정으로 **좌표계 상수가 전부 사라졌다.** 물체 좌표, 차량
좌표와 방향, 경로는 Host가 소유한다. 여기 남은 것은 Pi가 자기 센서로 재고
자기 팔로 집행하는 값뿐이다. 좌표 상수를 다시 여기 넣고 싶어지면, 그건
Host의 일이 Pi로 새는 신호다.
"""

# ── 파지 (사용자 지시) ─────────────────────────────────────────────────────
# GRASP 진입 후 베이스가 곧장 전진하는 거리.
#
# 이 전진의 목적은 "물체 가까이 가는 것"이 아니라 **물체를 벌어진 턱 사이로
# 밀어 넣는 것**이다(사용자 설명 2026-08-26). 팔은 이미 바닥 교시 자세로
# 내려와 있고 그리퍼는 열린 상태다. 그 상태로 차체가 전진하면 물체가
# 손가락 사이로 들어온다 — 평행 턱의 벌어진 목이 좌우 자기정렬 효과까지 낸다.
#
# ⚠️ 매우 예민한 값이다. 사용자가 "여러 번 실측할 필요가 있다"고 명시했고,
# **5cm로 바뀔 수 있다.** 실기 조정 전까지 이 값을 근거로 다른 수치를
# 유도하지 말 것.
GRASP_CREEP_FORWARD_MM = 100.0  # 지시(잠정 — 50.0 후보)

# 교시 파지 자세가 전제하는 물체 중심 위치(차체 전면 기준 전방).
# floor_grasp_profiles.GRASP_OBJECT_CENTER_FORWARD_MM과 같은 값이다.
#
# Pi는 이 정렬을 **판정하지 않는다** — Host가 오버헤드로 보고 GRASP 명령을
# 보낼 때 이미 맞춰 놓는다. 여기 남겨 두는 이유는 GRASP 시퀀스가 전제하는
# 배치를 문서로 못박아 두기 위해서다.
GRASP_OBJECT_CENTER_FORWARD_MM = 190.0  # 실측 2026-08-20

# 열린 그리퍼 최대 너비. floor_grasp_policy.GRIPPER_MAX_SAFE_OPEN_MM와 같은 값.
GRIPPER_OPEN_MM = 168.0  # 실측 2026-08-20

# 바닥 교시 자세에서 턱 중심이 차체 전면 앞으로 나가 있는 거리.
#
# 2026-08-26 통주행에서 역산했다 — 물체를 190mm에 놓고 24mm 전진해 물렸으므로
# 턱 선은 약 166mm다. **n=1, 퀸 한 종류**이고 조작자가 "들어온 것 같다" 시점에
# 멈춘 값이라 보수적일 수 있다. 반복 실측 대상이다.
#
# ⚠️ 이 값은 **차체 전면 기준**이라 뎁스 카메라 값과 직접 비교하면 안 된다.
# 아래 JAW_LINE_DEPTH_FORWARD_M 주석 참고. 여기 남겨 두는 것은 기하를
# 문서로 붙잡아 두기 위해서다.
JAW_LINE_FORWARD_MM = 166.0  # 실측(n=1) 2026-08-26

# 같은 턱 선을, **클래스마다 따로**, 뎁스 카메라가 읽는 값으로 적은 것.
#
# ## 왜 차체 전면 기준 값을 못 쓰는가
#
# 뎁스 카메라의 전방 거리(observe_target의 forward_m)는 클래스별 K를
# base_link 기준 줄자 실측으로 잡았다. base_link는 차체 중심 근처이고
# JAW_LINE_FORWARD_MM은 차체 **전면** 기준이라, 둘 사이에 차체 절반 길이만큼
# 오프셋이 있다 — 잡으려는 영역의 깊이보다 큰 오프셋이다.
#
# ## 왜 하나로 못 쓰고 클래스마다 재야 하는가
#
# 클래스별 K의 정확도가 제각각이다. rook만 3점 최소제곱(0.40/0.70/1.04m)이고
# 나머지는 먼 거리 1점이라, 파지 거리대에서 배율 오차가 크다. 2026-08-25에
# 여섯 물체를 **같은 물리 18cm**에 놓았더니 queen 14.4 / rook 18.3 /
# knight 18.7 / soccer 25.6cm로 읽혔다.
#
# 턱 선을 하나로 공용하면 그 배율 오차가 전진 거리에 그대로 실린다 —
# 실제 전진 24mm여야 하는 상황에서 queen은 -17mm(움직이지 않음),
# soccer는 101mm(77mm 과잉 전진)가 나온다. 후자는 물체를 넘어뜨린다.
#
# **같은 클래스로 잰 턱 선을 빼면 배율 오차가 대부분 상쇄된다** — 같은
# 클래스, 같은 거리대에서 같은 방향으로 틀리기 때문이다. 위 예에서
# queen 19mm, soccer 34mm(실제 24mm)로 오차가 한 자릿수 mm로 줄어든다.
#
# ## 재는 법
#
# tools/grasp_geometry_calibrate.py --mode jaw --label <라벨>.
# 실제로 물리는 자리에 놓고 forward_m을 읽은 뒤, 전진 없이 닫아서 물리는지
# 확인한다. 값이 없는 클래스는 GRASP 정렬 판정을 하지 않고 Host에 넘긴다.
JAW_LINE_DEPTH_FORWARD_M = {
    # 2026-08-26 실측. 놓고, 전진 없이 닫아, 실제로 물리는지 확인한 값이다.
    #
    # 같은 날 0.1985에서도 물리기는 했는데(부하 0.0821) **턱 끝에 걸려
    # 들어올릴 때 미끄러졌다.** 0.1757은 제대로 물렸다(부하 0.0782).
    # 목표는 "물리는 자리"가 아니라 "제대로 앉는 자리"이므로 후자를 쓴다.
    "rook": 0.1757,
    # knight는 닫음/들어올림/CARRY 부하가 0.0782로 **한 번도 안 떨어졌다** —
    # 가장 깨끗한 실측이다.
    "knight": 0.1881,
    # queen은 닫음 0.0821 -> 들어올림 0.0626 -> CARRY 0.0626. 닫는 순간
    # 대비로는 떨어졌지만 들어올린 뒤로는 흔들리지 않았다 — 사용자 확인
    # "일단 잘 잡았어". 닫는 순간의 부하에는 눌러 들어가는 동적 성분이
    # 섞여 있어 들어올리면 어차피 내려앉는다.
    #
    # ⚠️ **유도값이다(실측 0.1421 / 0.807).** 원래 실측은 queen의 K가
    # 19.3% 틀린 상태에서 잰 것이라 그 배율이 그대로 실려 있었다
    # (perception_node의 queen K 주석 참고). K를 고쳤으므로 턱 선도 같은
    # 배율로 되돌린다 — 관측과 턱 선이 같은 척도 위에 있어야 뺄셈이
    # 의미를 갖는다는 이 표의 대전제 그대로다.
    #
    # 보정 뒤 rook의 0.1757과 0.4mm 안에서 만난다. 턱 선은 정의상 모든
    # 클래스에서 물리적으로 같은 자리이므로 이것이 보정이 옳다는 방증이다.
    # 그래도 --mode jaw로 한 번 확인할 것 — 유도값과 실측은 다르다.
    "queen": 0.1761,
}  # TODO — soccer

# 같은 물리 자리인데 클래스마다 다르게 읽힌다는 증거.
#
# 턱 선은 정의상 모든 클래스에서 **물리적으로 같은 자리**다. 그런데 rook은
# 0.1757, knight는 0.1881로 읽는다 — knight가 7.1% 멀게 본다. 2026-08-25에
# 같은 물리 18cm를 놓고 잰 차이(2.2%)보다 크다.
#
# 그래서 턱 선·좌우 영점을 클래스마다 따로 두는 지금 방식이 옳다. 하나로
# 공용했다면 knight에서만 13mm씩 어긋났을 것이다.')

# 턱 목의 깊이. 위 두 실측의 차이다(0.1985 - 0.1757).
#
# 물체가 턱 선보다 이만큼 앞에 있어도 **물리기는 한다** — 다만 끝에 걸려
# 미끄러진다. 그래서 이 값은 "전진 거리가 이만큼 모자라도 일단 물기는
# 한다"는 여유폭이지, 목표로 삼을 값이 아니다.
#
# ⚠️ **턱은 깊이 방향으로 물체를 끌어당기지 않는다.** 2026-08-26에 확인했다 —
# 평행 턱은 곧게 닫히므로, 손가락 끝에 있는 물체는 끝에서 그대로 물린다.
# 물체를 목 안쪽으로 데려오는 것은 오직 차체의 미세 전진이다. 좌우는 다르다:
# 두 손가락이 대칭으로 닫히므로 좌우로는 중앙으로 끌려온다.
JAW_THROAT_DEPTH_M = 0.023  # 실측 2026-08-26

# 물체가 턱 중심선에 정확히 있을 때 카메라가 읽는 좌우 값 — 즉 좌우 영점.
#
# ## 왜 0이 아닌가
#
# 2026-08-26에 세 클래스를 턱으로 직접 물려(= 좌우로는 반드시 중앙에 앉는다)
# 읽었더니 +29.5 / +24.0 / +31.4mm가 나왔다. 카메라가 턱 중심선에서 옆으로
# 약 30mm 떨어져 달려 있다는 뜻이다.
#
# ## 왜 클래스마다 따로 두는가
#
# 겉보기 좌우 값은 거리에 비례한다 — 카메라는 각도를 재고 거리를 곱해
# 좌우를 낸다. 그래서 클래스별 K의 배율 오차가 좌우 값에도 그대로 실린다.
# 위 세 값을 각 클래스의 배율(2026-08-25 실측 기준 rook 1.02 / queen 0.80 /
# knight 1.04)로 나누면 29.0 / 30.0 / 30.2mm로 **1.2mm 안에 모인다** —
# 같은 물리 오프셋이 배율 때문에 다르게 읽힌 것이다.
#
# 배율을 알아내 나누는 대신 **클래스별 영점을 그대로 빼면** 그 클래스의
# 배율이 상쇄된다. 턱 선과 같은 방식이다.
#
# ## 재는 법
#
# tools/grasp_geometry_calibrate.py --mode seat.
# 턱으로 물리면 두 손가락이 대칭으로 닫히므로 물체는 **좌우로는 반드시**
# 턱 중심에 앉는다(깊이는 아니다 — 아래 JAW_THROAT_DEPTH_M 주석 참고).
# 그래서 이 모드는 좌우 영점에는 믿을 만하고, 턱 선에는 못 쓴다.
DEPTH_LATERAL_TO_JAW_CENTER_M = {
    "rook": 0.0295,
    # ⚠️ 유도값(실측 0.0240 / 0.807) — 턱 선과 같은 이유다. 보정 뒤 세 값이
    # 29.5 / 31.4 / 29.7 mm로 1.9mm 안에 모인다. 카메라가 턱 중심에서 옆으로
    # 약 30mm 떨어져 있다는 하나의 물리량으로 수렴한 것이다.
    "queen": 0.0297,
    "knight": 0.0314,
}  # 실측 2026-08-26 (n=1씩, queen만 K 보정으로 되돌린 유도값)

# servo 1 회전축에서 턱 중심까지의 수평 거리.
#
# 좌우 보정각을 atan2(오차, 이 값)으로 내므로 이게 없으면 **보정을 못 한다**.
# 지어낸 값을 쓰면 엉뚱한 각도로 턱을 돌리게 되므로 None으로 둔다 — 그동안은
# 치우친 물체를 Host에 넘긴다(grasp_alignment.judge 참고).
#
# 실측 2026-08-26: 바닥 파지 자세에서 servo 1을 ±12도 돌려 턱 중심이 바닥에
# 그린 두 점 사이가 125mm였다. reach = 125 / (2 tan12도) = 294.0mm.
#
# 이 값이 뜻하는 것:
#   1도당 좌우 5.1mm · 1 raw(0.088도)당 0.45mm — 데드밴드 없는 고운 분해능
#   서비스 한계 15도에서 최대 보정 ±79mm
#
# 마지막 줄이 중요하다. 턱 폭 허용치(체스말 기준 ±76mm)와 거의 같아서,
# **턱 폭 안에 들어온 물체는 servo 1으로 전부 중앙에 맞출 수 있다.**
# 한계각에 걸리는 경우와 영역 밖인 경우가 사실상 같은 지점에서 갈린다.
SERVO1_AXIS_TO_JAW_MM = 294.0  # 실측 2026-08-26

# 이 안에 들면 "가운데"로 본다. 더 좁히면 매번 보정을 돌게 되고, 넓히면
# 사용자 지시("최대한 가운데")에서 멀어진다.
GRASP_CENTERING_TOLERANCE_M = 0.010  # 지시(잠정)

# ── 파지 판정 ─────────────────────────────────────────────────────────────
# 그리퍼가 무언가를 물고 있다고 볼 부하 하한.
#
# ## 판독의 성질 (2026-08-26 실측)
#
# servo 6의 load_ratio는 **1/256 양자화**돼 있다 — 관측값이 전부 0.0039의
# 배수다. 그리고 **한 버스트 안에서는 완벽히 고정, 버스트 사이에서는
# 떠돈다.** 1.5초 동안 5표본을 떠도 편차가 0.0000인데, 자세를 옮기거나
# 그리퍼를 다시 명령하면 한두 양자씩 옮겨 앉는다. 마지막 정지 토크를 래치해
# 두고 다음 명령에서 다시 잡는 거동으로 보인다.
#
# ⚠️ **설명 변수를 찾지 못했다.** 두 번 헛짚었으니 기록해 둔다:
#
#   자세 가설 — 7개 자세 x 2개 폭을 돌았더니 편차 0.0000. 아니다.
#               (그리퍼는 손목 끝단이라 팔의 중력 성분을 안 탄다)
#   폭   가설 — 여섯 프로필의 닫힘 폭을 훑었으나 단조성이 없고, 같은 9.5mm가
#               한 회차엔 0.0391, 다음 회차엔 0.0313이었다. 아니다.
#
# 다음에 이 값을 손볼 사람은 원인을 다시 찾으려 하기 전에 위 둘이 이미
# 배제됐다는 것부터 보라. 원인을 모르는 채로도 임계는 정할 수 있다.
#
# ## 두 띠가 겹치지 않는다 — 다만 간격이 2양자뿐이다
#
#   빈손  0.0235 ~ 0.0430   ( 6/256 ~ 11/256, 자세·폭 전부 포함)
#   파지  0.0508 ~ 0.0978   (13/256 ~ 25/256, 2026-08-26 실기 전체)
#
# 두 띠 사이가 **2양자**다. 중점을 잡으면 양쪽에 1양자씩 남는다. 얇지만
# 이것이 데이터가 허락하는 전부다.
#
# ⚠️ 파지 쪽 하한 0.0508은 **INSERT 시점의 퀸**이다. 처음에 0.0626(n=3)으로
# 잡았다가 실기에서 그보다 3양자 낮은 값이 나와 임계를 내렸다. 이전 값
# 0.0528은 0.0508보다 **높아서** check_insert가 성공한 파지를 막았을 것이다 —
# 2026-08-26 통주행이 그 조건을 실제로 지나갔다(막히지 않은 것은 그 경로가
# 아직 실기에 안 붙어 있었기 때문이다).
#
# ## 부하는 빈손일 때만 자세와 무관하다
#
# 빈 그리퍼는 자세를 안 탄다(7개 자세에서 편차 0.0000). 그런데 **물체를 든
# 그리퍼는 자세를 탄다** — 같은 퀸이 CARRY에서 0.0547, DROP 자세에서
# 0.0508로 1양자 내려갔다. 팔이 전개되면서 물체 무게 벡터가 손목에 대해
# 회전하기 때문이라 물리적으로 당연하다.
#
# 그래서 임계는 **가장 낮게 읽히는 자세**(DROP)를 기준으로 잡아야 한다.
# CARRY에서만 재고 정하면 팔을 펼치는 순간 판정이 뒤집힌다.
#
# ⚠️ 여유가 양쪽 1양자뿐이라 **부하 단독으로는 못 믿는다.** 사용자 지시대로
# 파지 판정은 부하와 뎁스 카메라를 둘 다 요구하고, 그 요구가 여기서 보듯
# 여유가 아니라 필수다. 더 가벼운 물체를 쓰게 되면 두 띠가 겹칠 수 있고,
# 그때는 부하 조건을 판정에서 빼고 뎁스 카메라에만 기대야 한다.
# 값을 양자 격자 위에 정확히 올린다(12/256). 판독이 격자 위에서만 나오므로
# 임계도 격자 위에 두면 "같은 값일 때"의 거동이 반올림에 안 흔들린다.
LOAD_THRESHOLD = 12.0 / 256.0  # = 0.046875, 빈손 11/256과 파지 13/256의 중점

# 그리퍼가 **비어 있다**고 볼 부하 상한. GRASP 조건 판정에 쓴다 —
# 이미 무언가를 물고 있는데 또 파지하러 내려가면 물고 있던 것을 떨어뜨린다.
# LOAD_THRESHOLD와 같은 값을 쓰되 이름을 나눠 둔다: 하나는 "쥐었다"를,
# 다른 하나는 "비었다"를 묻는 서로 다른 판정이다.
EMPTY_LOAD_CEILING = 12.0 / 256.0  # = 0.046875

# ── 라이다 기하 (2026-08-26 실측) ──────────────────────────────────────────
# grippers_base/basket_lidar_align.py와 같은 값이어야 한다. 도메인 계층이
# ROS 패키지를 import하지 않으므로 여기 한 번 더 적는다.
LIDAR_HEIGHT_M = 0.140          # 실측 2026-08-26 (이전 0.091은 오측)
LIDAR_TILT_DEG = 11.3           # 실측 2026-08-26 — 정면 아래로 기울어져 있다
LIDAR_MIN_RANGE_M = 0.020       # 실측 — LDRobot LD19 (RPLidar A1이 아니다)

# ── 바구니 ────────────────────────────────────────────────────────────────
# INSERT로 넘어갈 때 **라이다가 읽어야 하는 거리**다.
#
# 차체 전면 기준이 아니라 라이다 판독값 기준인 것이 핵심이다. 바구니는
# 높이마다 앞뒤 위치가 다른 면이라(앞턱이 위쪽 벽보다 15mm 앞으로 나와 있다)
# "차체 전면에서 바구니까지"라는 단일 거리가 **정의되지 않는다**. 2026-08-26에
# 판으로 잰 오프셋 4.54cm와 바구니로 잰 1.9cm가 2.6cm 어긋난 것이 그 증거다.
# 라이다 판독값으로 직접 판정하면 중간 변수가 사라져 오차가 하나 줄어든다.
#
# 검증된 창은 [0.130, 0.139]다(나이트 0.1386, 퀸 0.1301 — 둘 다 투하 성공).
# 창의 위쪽 끝에 두는 이유: 약 0.125m에서 빔이 테두리를 넘어 바구니를 통째로
# 놓친다. 아래가 절벽이므로 붙이는 쪽이 아니라 떨어지는 쪽이 안전하다.
BASKET_STOP_LIDAR_M = 0.140  # 실측 2026-08-26

# INSERT를 허용하는 라이다 판독 하한.
#
# 약 0.125m에서 빔이 테두리를 넘어 바구니를 놓친다. 그 아래로는 판독값이
# **커지는 방향으로** 틀리므로(뒤쪽 벽이나 무반사) 거리 하한만으로는 못 막는
# 종류의 실패다. 그래도 하한을 두는 이유는, 절벽 아래를 읽고 있다는 것 자체가
# "지금 보는 면이 우리가 교정한 그 면이 아니다"라는 신호이기 때문이다.
# 절벽에서 3mm 띄운다.
BASKET_MIN_LIDAR_M = 0.128  # 실측 근거 2026-08-26

# 정지 판정의 허용 폭. 이 안에 들어오면 INSERT 조건 충족으로 본다.
# 데드밴드 때문에 한 번의 최소 버스트가 15mm라 그보다 잘게 못 쪼갠다.
BASKET_STOP_TOLERANCE_M = 0.015  # 실측 근거 2026-08-26

# INSERT 시점에 허용하는 정렬 오차. 2026-08-26 실기에서 +2.82도로 투하에
# 성공했다. 그 실측치에 여유를 두되, 검증된 범위를 크게 넘기지 않는다.
BASKET_YAW_TOLERANCE_RAD = 0.087  # 5.0도 — 실측 2.82도 성공에 여유

# INSERT 시점에 허용하는 좌우 오프셋.
#
# 바구니 반폭 121mm에서 물체 반폭과 여유를 뺀 값이다. 체스말(반폭 약 12mm)
# 기준 89mm, 축구공(23mm) 기준 78mm가 나오므로 낮은 쪽에 맞춘다.
#
# ⚠️ 이건 기하 계산이지 실측이 아니다. 실제로 투하해 보며 확인할 것 —
# 팔이 뻗는 지점이 정확히 중심선 위에 있다는 전제도 아직 미확인이다.
BASKET_LATERAL_TOLERANCE_M = 0.070  # 계산(잠정)

# 정면 피팅에 최소한 이만큼 점이 있어야 한다.
#
# 2026-08-26 검증 지점(0.130m)에서 97점이었다. 절반 아래로 떨어지면 빔이
# 테두리를 스치기 시작했다고 본다 — 완전히 놓치기 전에 걸린다.
BASKET_MIN_FACE_POINTS = 40  # 실측 근거 2026-08-26

# 연속 두 사이클의 라이다 판독이 이 안에서 일치해야 한다.
#
# 두 가지를 동시에 잡는다: 한 프레임 튐과 **차체가 아직 안 멈춤**. 후자가
# 중요하다 — 지금까지 "정지했는가"를 Host 명령으로만 믿고 있었는데, 이건
# 실제 증거다. 합의 속도 0.1m/s면 한 사이클에 10mm 움직이므로 5mm 창은
# 주행 중이면 반드시 걸린다.
BASKET_STABILITY_TOLERANCE_M = 0.005  # 지시

# 연속 두 사이클의 그리퍼 부하가 이보다 많이 떨어지면 미끄러지는 중이다.
GRIPPER_SLIP_LOAD_DROP = 0.010  # 지시(잠정)

# 바구니 테두리 높이.
BASKET_RIM_HEIGHT_M = 0.115  # 실측 2026-08-20

# 투하 자세에서 그리퍼 중심이 차체 전면 앞으로 뻗는 거리.
BASKET_DROP_REACH_FORWARD_MM = 200.0  # 실측 2026-08-20

# ── 링크 워치독 ────────────────────────────────────────────────────────────
# Host 명령이 이 사이클 수만큼 연속으로 안 오면 정지한다.
#
# "명령 없음"을 "직전 명령 유지"로 해석하면 링크가 끊겼는데 계속 굴러가는
# 사고가 난다. Host가 차량 제어를 소유한다는 것은 **Host가 말을 멈추면
# 차량도 멈춘다**는 뜻이기도 하다.
HOST_COMMAND_TIMEOUT_CYCLES = 3  # 지시

# ── 미션 진행 ──────────────────────────────────────────────────────────────
# 미션 한 번에 허용하는 파지 재시도 횟수.
MAX_GRASP_RETRY = 3  # 지시


def unresolved() -> dict:
    """아직 TODO인 상수 이름과 사유. 실기 투입 전 이 목록이 비어야 한다."""
    return {
        "GRASP_CREEP_FORWARD_MM": "100mm 대 50mm 미확정 — 반복 실측 필요",
        "JAW_LINE_DEPTH_FORWARD_M": "클래스별 미측정 — 없는 클래스는 정렬 판정을 못 한다",
    }
