"""Gripper_ArUco 설정 — 실측값은 전부 여기서만 고친다.

좌표계 (map frame)
    원점  : 가벽으로 두른 1.8 x 1.8 m 공간의 "앞쪽 왼쪽" 바닥 모서리
    +x    : 오른쪽 (0 ~ 1.800 m)
    +y    : 뒤쪽   (0 ~ 1.800 m). 카메라 A 가 선 변에서 멀어지는 방향
    +z    : 위쪽   (바닥 = 0)
    yaw   : +x 축 기준 반시계(CCW). 출력은 도(deg), 범위 -180 ~ +180
    단위  : 전부 미터(m). cm 로 잰 값은 /100 해서 적는다.

고칠 때는 🔴 표시가 붙은 값만 손대면 된다.
고친 뒤에는 `python selftest.py` 로 파일이 멀쩡한지 확인할 것.
"""

import math
from pathlib import Path

import numpy as np

# ---------------------------------------------------------------------------
# 1. 마커 딕셔너리 / ID 할당  (#175 확정)
# ---------------------------------------------------------------------------
ARUCO_DICT = "DICT_4X4_50"

ROBOT_MARKER_ID = 0                # 로봇 상판
FLOOR_MARKER_IDS = (1, 2, 3, 4)    # 바닥 기준점 4점

# ---------------------------------------------------------------------------
# 2. 마커 물리 크기 (m)
#    ⚠️ 검은 테두리의 "바깥 변" 길이다. 흰 여백은 빼고 잰다.
#       예: 종이가 10 x 10 cm 이고 사방 여백이 1 cm 면 → 0.080 을 적는다.
#
#    크기 정확도는 관대하다 — ±2 mm 어긋나도 위치 오차는 0.1 mm 도 안 늘어난다.
#    다만 잰 값을 정직하게 적어야 한다. 실제와 10 mm 이상 다르면 재투영 오차가
#    한계를 넘어 프로그램이 값을 거부한다.
#
#    로봇 마커(8 cm)는 상판 크기에 맞춰 줄인 값이다. 실제 검출기로 확인한 결과:
#      · 작업 영역 전 지점에서 검출됨 (화면에서 33 ~ 48 px)
#      · yaw 정밀도는 크기에 반비례하지만 8 cm 로도 목표 3° 에 여유가 크다
#        (6 cm → 95% 0.97° · 8 cm → 0.73° · 12 cm → 0.49°)
#      · 위치 정밀도는 네 모서리의 평균이라 크기와 무관하다
#      · 대가는 흔들림(모션블러) 내성이다 — 12 cm 는 꽤 흔들려도 잡히지만
#        8 cm 는 심하게 흔들리는 프레임에서 놓칠 수 있다. 로봇이 빠르게
#        움직이는 동안 잠깐 놓쳐도 POSE_HOLD_SEC 동안 마지막 값이 유지된다.
#
#    흰 여백은 검출에 거의 영향이 없었다 (0.5 cm 도 1.33 cm 도 10/10 검출).
#    1 cm 면 충분하다.
#
#    바닥 마커는 정지해 있어 흔들림 걱정이 없고 바닥 공간도 넉넉하므로
#    12 cm 를 유지한다. 검출 여유가 더 크다.
# ---------------------------------------------------------------------------
ROBOT_MARKER_SIZE = 0.080   # 🔴 검은 사각형 실측 (종이 10 cm - 여백 2 cm)
FLOOR_MARKER_SIZE = 0.120   # 🔴 검은 사각형 실측

# ---------------------------------------------------------------------------
# 3. 로봇 상판 높이 (m) — z 구속의 핵심 값
#
#    바닥에서 마커 인쇄면까지. 이 값이 틀리면 그대로 위치 오차가 된다.
#    10 mm 틀리면 위치가 12.8 mm 밀린다 → mm 단위로 실측할 것.
#    가장 자주 틀리는 값이다.
# ---------------------------------------------------------------------------
#    ⚠️ 현재 값은 로봇이 아니라 '대역물(책 더미)' 기준이다 (2026-08-23, 자로 잼).
#       로봇에 마커를 붙이면 반드시 다시 재서 넣을 것.
#
#    자로 재기 전에 값을 가늠하는 법 — 두 카메라가 마주보고 있어서, 이 값이 틀리면
#    두 대의 답이 '반대 방향으로' 벌어진다. 그래서 간격이 최소가 되는 높이를 찾으면
#    참값이 나온다. 실제로 두 번 다 자로 잰 값과 일치했다:
#         대역물 A: 교차법 270 mm ↔ 자로 잰 값 270 mm
#         대역물 B: 교차법 250 mm ↔ 자로 잰 값 250 mm
#    (200 mm 로 잘못 두었을 때 두 카메라 간격이 72 mm 였고, 250 으로 고치니 6 mm 가 됐다)
#    ⚠️ 이 마커는 수평이 아니다 — 실측 16.7° 기울어져 있고 바꿀 수 없다.
#       네 코너가 높이로 23.9 mm 흩어진다(258.5 ~ 282.3 mm).
#       그래도 동작하는 이유: 기울기가 중심 기준 대칭이라 코너 4개의 교점을
#       평균내면 오차가 대부분 상쇄된다. 단 **중심 높이를 정확히 넣어야** 한다.
#
#    270 mm 의 근거 — 서로 독립인 두 방법이 일치:
#        삼각측량(두 카메라로 코너 3D 복원, 가정 없음)  270.5 mm
#        교차법(두 카메라 간격이 최소가 되는 높이)        270 mm (간격 5.6 mm)
#        참고로 삼각측량이 복원한 마커 크기가 79.0 mm(실제 80)라 측정이 신뢰할 만하다.
ROBOT_MARKER_HEIGHT = 0.270   # 🔴 실측 — 로봇 부착 마커의 '중심' 높이

# ---------------------------------------------------------------------------
# 4. 작업 영역 — 로봇이 다니는 범위 (m)
#
#    가벽 1.8 x 1.8 m 를 전부 쓰지는 않는다. 앞뒤 400 mm 는 상자 자리로 뺀다.
#    상자 길이가 350 mm 이므로 여유 50 mm 를 더한 값이다.
#
#    카메라 쪽 제약도 함께 만족한다 — 로봇 마커가 지면이 아니라 200 mm 공중에
#    있어서 가벽(350 mm)이 시선을 막는데, 그 사각이 앞뒤 약 150 mm 다.
#    상자 자리 400 mm 가 그보다 넓으므로 저절로 피해진다.
#
#    ※ 지면(z=0)은 1.8 x 1.8 m 전체가 보인다. 이 제한은 공중에 뜬 로봇 마커에만
#      해당하며, 바닥 물체 탐색에는 영향이 없다.
# ---------------------------------------------------------------------------
WORKSPACE_X = (0.000, 1.800)   # 폭 1800 mm — 전부 사용
WORKSPACE_Y = (0.400, 1.410)   # 세로 1010 mm — 앞뒤 400 mm 씩 상자 자리
                               # 뒤쪽 경계는 바닥 마커 3·4 의 y 와 맞춘다.
                               # 간격 실측(1010 mm)으로 마커가 1.410 으로 옮겨져 함께 갱신.


def in_workspace(x: float, y: float) -> bool:
    """로봇이 작업 영역 안에 있는가."""
    return (WORKSPACE_X[0] <= x <= WORKSPACE_X[1] and
            WORKSPACE_Y[0] <= y <= WORKSPACE_Y[1])


# ---------------------------------------------------------------------------
# 5. 바닥 기준점 4개의 map 좌표 (m) — 마커 "중심" 위치
#
#    이 4점이 좌표계를 정의한다. 작업 영역의 경계가 아니라 기준점일 뿐이다.
#    실제로 붙인 자리를 재서 넣을 것. 여기 적은 값이 곧 "정답"이 된다.
#    make_layout.py 가 잰 값을 이 형식으로 바꿔 준다.
#
#    재기 오차는 거의 1:1 로 위치 오차가 된다 (5 mm 로 재면 5 mm). 5 mm 이내 권장.
#
#    자리를 바꿀 때 지켜야 할 세 가지
#      (1) 넓게 벌릴수록 정확하다 — 가로 100 cm → 4.3 mm, 160 cm → 2.7 mm.
#          160 cm 를 넘으면 이득이 거의 없다.
#      (2) 세로는 y ∈ [0.30, 1.50] 안에 둘 것.
#          밖으로 나가면 가벽에 가려 한쪽 카메라가 그 마커를 놓친다.
#      (3) 상자와 좌우(x) 위치가 겹치면 안 된다.
#          상자(높이 220 mm)는 지면에 약 200 mm 짜리 그림자를 드리우고,
#          마커가 그 안에 들어가면 그 카메라는 마커를 통째로 못 본다.
#          겹칠 수밖에 없다면 상자에서 앞뒤로 250 mm 이상 떨어뜨릴 것.
#
#    🔴 붙이는 "방향"도 값만큼 중요하다 — 네 장 모두 인쇄물의 윗변이 +y 를 향하게,
#       같은 방향으로 붙일 것. ArUco 는 마커 자신의 좌상 코너부터 순서대로 돌려주므로,
#       한 장이라도 90도 돌아가 있으면 코너 대응이 어긋나 자세 해가 통째로 틀어진다.
#       (증상: reproj 가 수십 px 로 뜨고 pose 가 안 잡힘 → 방향부터 의심할 것)
# ---------------------------------------------------------------------------
#    자로 잰 값 (2026-08-23). 1번을 기준점으로 두고 나머지를 1번에서 쟀다.
#        1↔2  1610 mm      1↔3  1010 mm      1↔4  1900 mm (대각)
#    대각 검산: sqrt(1610² + 1010²) = 1900.6 mm — 실측 1900 mm 와 0.6 mm 차.
#    네 점이 직사각형을 이룬다는 것이 독립적으로 확인된 셈이다.
FLOOR_MARKER_WORLD = {          # 🔴 실측
    1: (0.100, 0.400),   # 앞쪽 왼편 — 기준점 (고정)
    2: (1.710, 0.400),   # 앞쪽 오른편 — 1번 + 1610
    3: (0.100, 1.410),   # 뒤쪽 왼편   — 1번 + 1010
    4: (1.710, 1.410),   # 뒤쪽 오른편
}

# ---------------------------------------------------------------------------
# 6. 상자 — ArUco 없이 좌표 직접 지정 (#175: 마커는 환경·장비에만)
#
#    실측 크기 (m): 폭 x 길이 x 높이 = 210 x 350 x 220 mm
#      폭(BOX_W) = x 방향, 길이(BOX_L) = y 방향, 높이(BOX_H) = z 방향
#
#    두 개 모두 뒤쪽 띠에, 가운데(x=0.9)에서 좌우 같은 거리로 놓는다.
#    이격 거리를 바꿀 때의 한계:
#        0.35 ~ 0.45 m  적당 (마커까지 28 ~ 18 cm 여유)
#        0.55 m 부터    로봇이 상자 앞에 설 때 마커를 밟을 수 있다
#        0.63 m 초과    상자가 마커와 물리적으로 겹친다
#
#    y = 1.625 는 뒤쪽 벽에 딱 붙인 위치다 (y 1.450 ~ 1.800 차지).
#    앞으로 당길수록 그림자가 길어지므로 벽에 붙이는 게 좋다.
#
#    yaw 180 = 작업 영역 쪽을 향해 열려 있다.
# ---------------------------------------------------------------------------
BOX_W, BOX_L, BOX_H = 0.210, 0.350, 0.220

#    ⚠️ 여기 적는 y 는 상자 "중심"이지 "입구"가 아니다. 175 mm (= BOX_L/2) 차이가
#       나므로 헷갈리면 로봇이 상자 한가운데로 들어가려 한다.
#
#       현장 실측(2026-08-23)은 입구 기준으로 잡혀 있고, 서로 일치한다:
#           토이  : 격벽 왼쪽  제일 위(0.000, 1.800) 에서 x +45 cm, y −35 cm
#                   → 입구 중앙 (0.450, 1.450) → 중심 y 1.450 + 0.175 = 1.625 ✓
#           체스  : 격벽 오른쪽 제일 위(1.800, 1.800) 에서 x −45 cm, y −35 cm
#                   → 입구 중앙 (1.350, 1.450) → 중심 y 1.625 ✓
BOXES = {                       # 🔴 실측 (상자 "중심" 좌표)
    "toy":   (0.450, 1.625, 180.0),   # 입구 중앙 (0.450, 1.450) · 가운데에서 왼쪽 45 cm
    "chess": (1.350, 1.625, 180.0),   # 입구 중앙 (1.350, 1.450) · 가운데에서 오른쪽 45 cm
}

# ---------------------------------------------------------------------------
# 상자 앞 정지 지점
#
#    ⚠️ localizer 가 내놓는 좌표는 로봇 "마커 중심"이지 차체 앞쪽이 아니다.
#       주행 목표를 줄 때 이 차이를 빼지 않으면 로봇이 상자에 박는다.
#
#    이 값은 **현장에서 직접 잰 것**이다 (2026-08-23). 로봇을 원하는 자리에
#    세워 놓고 카메라로 마커 중심 위치를 읽었다:
#
#        마커 중심 y 1306.4   상자 입구 y 1450.0   →  차이 143.6 mm
#        (205 프레임 · 흔들림 0.1 mm · 두 카메라 차이 7.5 mm)
#
#    ⚠️ "차체 앞과 상자 사이 간격" 과 "마커 중심에서 범퍼까지" 로 쪼개지 않는다.
#       쪼개려면 범퍼 위치를 자로 재야 하는데 현장에서 그게 어렵고, 제어기가
#       필요한 건 합뿐이다. 하나로 두면 틀릴 여지도 하나로 준다.
#
#    두 상자 모두 뒤쪽 벽에 붙어 −y 로 열려 있어 접근 기하가 같으므로
#    같은 값을 쓴다. 배치가 바뀌면 다시 재야 한다.
# ---------------------------------------------------------------------------
BOX_APPROACH_FROM_ENTRANCE = 0.144   # 🔴 실측 — 마커 중심이 상자 입구에서 떨어질 거리

# 로봇 자체의 기하 — 마커 중심에서 범퍼 맨 앞까지 (전진 방향 수평 투영, m)
#
#    위 실측을 쪼개서 얻은 값이다 (2026-08-23):
#        마커 중심 → 입구   143.6 mm  (카메라 실측)
#        차체 앞  → 바구니     45 mm  (자로 잼)
#        차이               98.6 mm  ← 이 값
#
#    ⚠️ 상자 접근에는 쓰지 않는다. 거기엔 BOX_APPROACH_FROM_ENTRANCE 를 그대로
#       쓴다 — 직접 잰 값이라 오차가 전파되지 않는다. 이 값은 다음 용도다:
#         · 바닥 물체에 접근할 때 (그리퍼 도달 거리로 별도 여유를 잡아야 함)
#         · 충돌 판정 — 범퍼가 어디인지
#    정확도는 ±10 mm 정도로 본다 (두 카메라 차이 7.5 mm + 자 측정 오차).
ROBOT_MARKER_TO_FRONT = 0.099   # 🔴 실측에서 역산

# ---------------------------------------------------------------------------
# 7. 카메라
#
#    배치 (2026-08-23 실측, 두 대 모두 삼각대 높이 160 cm 로 맞춤):
#
#                     cam0              cam1
#        map 위치     (0.950, 1.753)    (0.875, -0.061)
#        추정 높이    1.560 m           1.551 m      (삼각대 실측 1.600)
#        하향각       65.2°             58.8°
#        바닥 마커    4/4 · 100%        4/4 · 100%
#        가장자리여유 115 px            117 px
#        초점(FOCUS)  5                 0
#
#    하향각이 6.4° 다른 것은 **의도된 것**이다. 두 대 모두 물건을 내려놓을
#    상자(BOXES, y = 1.625)를 봐야 하는데 상자와의 거리가 크게 다르다.
#    cam0 는 상자 바로 뒤(y 1.753)라 깊게 숙이고, cam1 은 1.7 m 건너편이라
#    덜 숙여야 상자가 화면에 들어온다. 대칭으로 맞추면 안 된다.
#
#    ⚠️ cam1 은 처음에 높이 1.27 m 였고, 그때 1번 마커의 화면 가장자리 여유가
#       8 px 였다 — 삼각대를 스치기만 해도 마커를 통째로 잃는 상태였다.
#       160 cm 로 올려 하향각이 깊어지자 여유 8 → 117 px 로 회복됐다.
#       두 대의 마커 크기 분포가 거울 대칭(가까운 쪽 125~129 px · 먼 쪽 93~97 px)
#       인 것이 마주보는 배치가 제대로 잡혔다는 신호다.
#
#    ⚠️ 삼각대 다리 위치를 바닥에 표시할 것. 측정 도중 카메라를 스쳤더니
#       검출률이 78~92% 로 떨어지고 yaw 산포가 8.85° 로 FAIL 났다.
#       손 떼고 다시 재니 505 프레임 100% 였다.
#
#    이 배치(후퇴 0 m)는 selftest.py 시뮬레이션 기준 하향각 40°~78°
#    어디를 넣어도 작업 영역 사각지대가 0 이다 — C270 때(42.8°에서
#    한쪽으로만 5°만 더 숙여도 8% 손실)보다 훨씬 관대하다. 실제로
#    "사각이 더 없어졌다"고 확인됨. 정확한 각도가 궁금하면 각도기 앱으로
#    재서 selftest.py --tilt 로 확인할 것 — 40~78° 안이면 안전하다.
#
#    좌우는 그대로 관대하다 (10 cm 어긋나도 99.7% 유지, C270 때와 동일
#    가정 — HFOV 가 넓어져 더 관대해졌으면 관대해졌지 나빠지지 않는다).
#
#    C270 때는 후퇴가 0 이라 한 대가 바닥 기준점 4개를 동시에 못 봤는데
#    (항상 2개만), C920 은 화각이 넓어 **두 대 모두 4개를 100% 본다**
#    (2026-08-23 실측, 1050 프레임 전부). MIN_FLOOR_MARKERS=2 는 여유로 남겨 둔다.
# ---------------------------------------------------------------------------
CAM_INDICES = (0, 1)         # 🔴 cv2.VideoCapture 인덱스
# C920 1080p. 720p 대비 선형 1.5 배다.
#
# ⚠️ 해상도를 바꾸면 아래 세 가지가 한 세트로 따라와야 한다.
#      1) 이 값
#      2) 캘리브레이션 재촬영 — K 는 픽셀 단위라 해상도가 바뀌면 통째로 무효다
#         (Camera.load 가 image_size 를 검사해 안 맞으면 거부한다)
#      3) 아래 재투영 임계값 — 픽셀 단위라 같은 각도 오차가 1.5 배로 찍힌다
#
# 실측 대가: 1080p 는 10 fps → 5 fps 다. DSHOW 가 MJPG 요청을 무시해서
# YUY2 로 고정되고, 대역폭이 프레임률을 반으로 깎는다. 얻는 것은 바닥 마커가
# 91 px → 136 px 로 커지는 것(검출 여유). 잃는 것은 시간 해상도다.
IMG_W, IMG_H = 1920, 1080
# 근사 수평화각. 캘리브레이션 파일이 없을 때 approx_camera_matrix() 가 쓴다.
#
# 공칭 70.4 대신 **실측한 이 개체들의 값 68.5** 를 쓴다. 지금 1080p 캘리브레이션이
# 없어 근사 K 로 도는 중이라, 이 값이 정확도를 직접 좌우한다.
#
# 근거 세 가지가 모두 68.2~69.3 을 가리킨다:
#   1) 삼각대 실제 높이 1600 mm 로 판정 — 68.5 에서 cam0 1607 · cam1 1595 (평균 1601)
#      70.4 에서는 1557 / 1549 로 43~51 mm 낮게 나온다
#   2) 캘리브레이션 4회: 68.3 · 68.9 · 69.2 · 69.3
#   3) cam1 의 바닥 reproj 최소점: 68.5
#
# ⚠️ reproj 만 보고 고르면 안 된다 — cam0 는 66 도 이하까지 계속 좋아지는데
#    그때 높이 추정이 1674 mm 로 실측(1600)과 어긋난다. 초점거리와 축척이
#    서로 상쇄되는 평면 PnP 축퇴 때문이다. 독립된 실측값(삼각대 높이)이 판정한다.
HFOV_DEG = 68.5              # 🔴 실측 (공칭 70.4)

# calib/cam0.npz, calib/cam1.npz
# 이 파일이 있는 폴더 기준으로 고정한다. "calib" 처럼 상대경로로 두면 실행 위치(cwd)에
# 따라 엉뚱한 폴더에 저장되고, localizer.py 가 읽을 때도 못 찾는다.
# (실제로 cam1.npz 가 grippers/calib/ 에 저장된 적이 있다.)
CALIB_DIR = str(Path(__file__).resolve().parent / "calib")

# ---------------------------------------------------------------------------
# 8. 자세 추정 / 폴백
# ---------------------------------------------------------------------------
# 마커 로컬 +x 축과 로봇 전진 방향의 차이(도).
#
# 실제 ArucoDetector 로 확인한 코너 순서(똑바로 놓은 마커 기준):
#     코너[0] 위왼쪽 → 코너[1] 위오른쪽 → 코너[2] 아래오른쪽 → 코너[3] 아래왼쪽
# 코너[0] 은 이미지가 아니라 "마커 무늬 자신"을 따라간다. 종이를 돌리면 코너[0]
# 도 같이 돈다 — ArUco 는 마커의 방향을 스스로 안다.
#
# localizer 는 yaw 를 코너0→코너1 방향, 곧 "종이의 오른쪽" 방향으로 잰다.
# 로봇에는 "종이 위쪽 = 로봇 앞쪽"으로 붙이는 게 직관적이라 그 차이 90도를
# 기본값으로 둔다.
#     종이 위쪽 = 로봇 앞쪽   →  90.0  (기본값, 권장)
#     종이 오른쪽 = 로봇 앞쪽 →   0.0
# 다르게 붙였으면 사용법.txt 9단계의 보정 절차로 이 값을 구하면 된다.
YAW_OFFSET_DEG = 90.0        # 🔴 마커를 다르게 붙였다면 수정

# 마커가 안 보일 때 마지막 값을 유지할 최대 시간(초). 넘으면 pose.ok = False.
POSE_HOLD_SEC = 1.0

# 외부파라미터를 풀기 위한 최소 바닥 마커 개수 (4장이 이상적)
MIN_FLOOR_MARKERS = 2

# 외부파라미터 재투영 오차가 이 값(px)을 넘으면 그 프레임은 버린다.
# 이 값은 "얼마나 잘 쟀는지"의 지표이기도 하다 (아래 기준은 720p 환산):
#     1.0 미만 → 재기 오차 2~3 mm 수준 · 2.0 넘음 → 10 mm 이상, 다시 잴 것
#
# ⚠️ 픽셀 단위라 해상도에 비례한다. 같은 각도 오차가 1080p 에서는 1.5 배로 찍히므로
#    720p 기준 3.0 은 1080p 에서 4.5 다. 안 올리면 실제로는 좋아졌는데도 임계값을
#    못 넘어 ready=False 가 되고 위치가 아예 안 나온다.
MAX_EXTRINSIC_REPROJ_PX = 4.5      # 720p 기준 3.0 × 1.5

# 외부파라미터 고정(lock)
#
# 카메라는 삼각대에 고정되어 있으므로 "카메라가 map 어디에 있는지"는 한 번만
# 풀면 된다. 시작할 때 4장이 모두 보이는 프레임을 이만큼 모아 코너를 평균낸 뒤
# 한 번 풀고 고정한다. 평균 덕에 노이즈가 줄어 매 프레임 푸는 것보다 정확하고,
# 고정 후에는 바닥 마커가 다 가려져도 로봇 추적이 이어진다.
#     매 프레임 재계산 : 오차 평균 0.77 mm · 최대 2.14 mm
#     고정 사용        : 오차 평균 0.29 mm · 최대 0.70 mm
# 0 으로 두면 고정하지 않고 매 프레임 다시 푼다.
EXTRINSIC_LOCK_FRAMES = 30

# 고정 후에도 매 프레임 검산한다. 보이는 바닥 마커의 재투영 오차가 이 값을 넘는
# 프레임이 연속으로 쌓이면 "카메라가 움직였다"고 보고 고정을 푼다.
EXTRINSIC_RELOCK_PX = 6.0          # 720p 기준 4.0 × 1.5 (위와 같은 이유)
EXTRINSIC_RELOCK_FRAMES = 15


# ---------------------------------------------------------------------------
# 파생값 — 아래는 건드리지 않는다
# ---------------------------------------------------------------------------
def floor_object_points() -> tuple[np.ndarray, dict[int, np.ndarray]]:
    """바닥 마커들의 map 좌표계 코너 4점 묶음 (z = 0).

    바닥 마커는 눕혀 붙이므로 네 코너 모두 z = 0 이다.
    마커 하나당 4점이므로 4장이면 16점 — 외부파라미터가 훨씬 안정된다.
    코너 순서는 ArUco 검출 순서와 같다: 좌상, 우상, 우하, 좌하
    (인쇄물 윗변이 +y 를 향하게 붙였다는 전제)
    """
    per_id = {}
    h = FLOOR_MARKER_SIZE / 2.0
    for mid, (cx, cy) in FLOOR_MARKER_WORLD.items():
        per_id[mid] = np.array([
            [cx - h, cy + h, 0.0],
            [cx + h, cy + h, 0.0],
            [cx + h, cy - h, 0.0],
            [cx - h, cy - h, 0.0],
        ], dtype=np.float64)
    allpts = np.vstack([per_id[m] for m in sorted(per_id)])
    return allpts, per_id


def approx_camera_matrix(w: int = IMG_W, h: int = IMG_H,
                         hfov_deg: float = HFOV_DEG) -> np.ndarray:
    """캘리브레이션 파일이 없을 때 쓰는 근사 내부파라미터.

    ⚠️ 배선 확인용 임시값이다. 정확도를 맞추려면 calibrate_camera.py 를 돌릴 것.
    """
    fx = (w / 2.0) / math.tan(math.radians(hfov_deg) / 2.0)
    return np.array([[fx, 0.0, w / 2.0],
                     [0.0, fx, h / 2.0],
                     [0.0, 0.0, 1.0]], dtype=np.float64)
