# servo 1(팔 베이스 요)을 현재 위치에서 offset_rad만큼 돌린다.
#
# GRASP 진입 전 좌우 보정 전용이다 — 물체가 턱이 쓸고 갈 영역 안에 있지만
# 가운데가 아닐 때, Pi가 스스로 고치는 수단이다(사용자 지시 2026-08-26).
# 메카넘 옆걸음을 쓰지 않는 이유는 데드밴드 때문에 최소 이동이 15mm라
# 고치려는 오차보다 커서다.
float32 offset_rad
---
bool ok
string message
int32 position_raw    # 이동 후 servo 1 실측 위치
